# Kisaki 隐私说明

最后更新：2026-08-27

Kisaki 是本地优先的桌面应用，不包含广告、遥测或使用统计 SDK，也不会自动上传运行日志。

## 本地保存的数据

应用会在本机保存设置、聊天会话、角色文件、窗口状态和诊断日志。API 凭据优先保存在操作系统密钥链；密钥链不可用时，会回退到本地加密存储。

## 发送给外部服务的数据

只有在用户启用并使用相应功能时，Kisaki 才会直接连接用户选择或配置的服务：

- AI 服务会收到 API 凭据、模型名、角色系统提示词、必要的会话历史、用户输入以及工具结果。
- 当用户授权 AI 读取文件时，相关文件内容或摘要可能作为工具结果发送给 AI 服务。
- TTS 服务会收到 API 凭据、待合成文本、模型和音色参数。
- 搜索及天气服务会收到 API 凭据（如需要）、查询词、城市或时间范围等参数。
- 自动更新会请求 Kisaki 的 GitHub Releases 更新清单并下载用户确认的更新包。

这些服务可能根据各自的隐私政策记录网络地址、请求元数据或内容。Kisaki 的维护者不代理这些请求，也不会收到用户的 API 凭据；用户应选择可信的服务地址并阅读其政策。

## 日志

本地日志用于诊断故障，按大小轮转，不会自动上传。日志可能包含服务地址、模型名、角色名、工作目录或查询词，但不应包含完整 API Key。分享日志前请检查其中是否含有敏感路径或内容。

## 删除数据

应用内的“清除所有本地数据”会删除角色、会话、设置、日志、缓存备份以及 Kisaki 创建的系统密钥链条目。卸载程序是否删除应用数据取决于操作系统和安装器；需要彻底清除时，请先使用应用内清除功能。

