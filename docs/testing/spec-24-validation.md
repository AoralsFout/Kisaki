# spec #24：Rust 组合根验证记录

- 日期：2026-09-12；桌面人工冒烟于 2026-09-15 补充完成。
- 需求：[spec #24](https://github.com/AoralsFout/Kisaki/issues/24)，实施工单 #30–#36。
- 基线：`20b1cce1642e30c29a989a6e2ff347b644bb3cec`。
- 交付：[PR #37](https://github.com/AoralsFout/Kisaki/pull/37)。CI 状态以 PR 当前提交为准，不以旧提交的成功代替当前结果。

## 自动验证方法

在仓库根目录运行：

```powershell
cargo test --manifest-path src-tauri/Cargo.toml --locked --all-targets
cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings
npm test
npm run build
npx tauri build --no-bundle
```

架构检查是 Rust 测试的一部分，使用语法结构而不是行数或整段源码快照。它针对生产代码检查全局状态、目录兜底与组合根边界，测试模块及真实临时目录 fixture 不作为生产装配点。

## 行为覆盖

| 范围 | 已有自动回归覆盖 |
| --- | --- |
| 目录装配 | dev / 生产布局、六类位置首次创建、既有数据保留、逐个目录不可用时的位置和底层原因 |
| 会话 | 首次读取、真实读写、原子覆盖、清空、旧会话文件保留及文件系统失败 |
| 角色与角色包 | 文件和图片操作、列表、删除、Live2D 导入、包往返、非法路径与非法包、同名角色的独立目录隔离 |
| 应用数据 | 导出与导入往返、替换前恢复备份、非法归档、文件系统失败、开发模式禁止重置及原有删除范围 |
| 工作目录授权 | 原生选择后的签发与解析、真实落盘、同文件重启恢复、同根不同能力 id、撤销与写盘失败 |
| 路径与检查点 | 穿越、绝对路径、非目录、Windows 数据流、符号链接 / junction 逃逸、备份回档与清理 |
| 执行 | 摘要绑定、一次性批准、60 秒 / 5 分钟边界和过期、拒绝、真实输出、主动取消、撤销联动、超时及失败后的清理 |
| 执行输出 | 流式事件、输出裁剪、尾部、注入目录和日志保留；同根不同能力运行中不误取消 |
| 日志 | 原生与 WebView 写入、混合并发、v2 字段、分页、导出、轮转、清理失败，以及 panic hook 的默认输出与忙锁行为 |

授权和执行测试各自构造状态，不依赖全局初始化顺序。TTL 使用注入的单调时钟，不等待真实的 60 秒或 5 分钟。执行测试仍启动真实子进程；取消和失败路径也会回收测试启动的进程。

收口代码的本地 Rust 回归为 **129 项业务测试＋18 项结构化架构检查，共 147 项通过**；前端为 **98 个文件、694 项测试通过**。架构检查与业务测试使用同一个 Rust 测试命令执行。

## 契约与删除范围核对

- 对照基线的 **53 个 Tauri 命令签名及 53 个注册入口**，仅排除框架注入的状态参数；命令名字、其余参数和返回类型一致。
- 原有 JSON / ZIP 格式与日志 v2 字段由行为测试覆盖。`kisaki-execution-output` 和 TTS Channel 的既有事件契约不变。
- 前端业务源码、Tauri 窗口配置、能力配置、托盘与光标轮询实现不因这次装配改动而重写。
- 重置仍只清理原有角色、日志、会话及检查点备份范围，不扩大到授权记录、凭据或执行输出。

## Windows CI 的测试 fixture 修正

旧的执行撤销测试在 Windows CI 连续两次等待就绪超时，而本机通过。仅将就绪输出从 `Write-Output` 改为 `Console.Out.WriteLine` 加显式刷新后，对照提交 `ee509db` 的三个平台 CI 全部通过。

扩大执行覆盖后，Windows CI 又暴露了测试释放条件对 PowerShell cmdlet 的依赖；加入 `Get-Location` 的诊断还会让阻塞提前到就绪阶段。测试 fixture 因而改用 `Console`、`File.Exists` 和 `Thread.Sleep` 的 .NET API，不依赖被清理环境下的 cmdlet / 模块装载。未放宽超时，未更改生产环境清理、安全规则或执行方式。临时目录探针已删除，失败时的命令与输出诊断保留。该修正的提交 `f7f8f9c` 已通过 [Windows、Linux、macOS 完整 CI](https://github.com/AoralsFout/Kisaki/actions/runs/34687325905)。

## 桌面启动入口复核与手工冒烟

2026-09-15，用户完成桌面人工冒烟并确认未发现问题。下表继续区分自动证据与人工结果；目录装配失败的破坏性场景只使用自动测试和源码接线复核，不在真实用户数据上注入故障。

| 场景 | 自动证据与源码复核 | 人工结果 |
| --- | --- | --- |
| 启动成功 | 目录装配行为测试；应用编译验证 | 通过：主窗口与会话列表可用 |
| 目录装配失败 | 各位置不可用的自动测试，断言错误包含路径及原因 | 未对真实用户数据注入故障；自动回归通过 |
| 主窗口与设置窗口 | 两者仍从同一 Tauri 应用取得状态，未增加窗口隔离 | 通过：设置窗口开关后主窗口、既有选择与会话正常 |
| 系统托盘 | 托盘注册与既有实现保持接线 | 通过：隐藏、唤回与退出正常 |
| 全局快捷键 | Alt+K 注册与 toggle 路径保留；失败日志明确传入目录 | 通过：Alt+K 隐藏与唤回正常 |
| 单实例唤回 | 单实例插件回调仍显示、还原并聚焦主窗口 | 通过：重复启动未产生第二份进程状态 |
| 更新入口 | updater / process 插件及配置保持不变 | 通过：检查更新入口正常，未执行实际安装 |
