# 桌宠项目规划文档

> 一款桌面透明桌宠，支持 AI 对话与 Agent 能力。
> 基于 Tauri v2 + Vue 3 + TypeScript

---

## 一、项目现状

| 模块 | 状态 |
|------|------|
| 透明无边框窗口 | ✅ 已完成（`transparent: true`, `alwaysOnTop: true`） |
| 窗口拖拽移动 | ✅ 已完成（Tauri 拖拽区域） |
| 角色立绘显示 | ⚠️ 基础（静态 PNG，可切换姿势） |
| AI 对话 | ❌ 未实现 |
| Agent 能力 | ❌ 未实现 |
| 交互菜单 | ❌ 未实现 |
| 系统托盘 | ❌ 未实现 |

### 现有资源

- **角色立绘**：`public/character/kisaki/` 下的 PNG 图片
  - KisakiA 系列（A 姿势）：`kisakiA_aa` ~ `kisakiA_al`（~37 张，无边框）
  - KisakiB 系列（B 姿势）：`kisakiB_aa` ~ `kisakiB_br`（~43 张，带边框）
  - **注意**：这些是独立的整幅立绘，并非动画序列帧，无法连续播放
- **当前窗口**：800×600，无边框透明，始终置顶

---

## 二、整体架构

```
┌────────────────────────────────────────────────────────────┐
│                      Tauri 桌面层                          │
│  ┌──────────────┐  ┌───────────┐  ┌───────────────────┐   │
│  │ 透明窗口管理  │  │ 系统托盘  │  │ 全局快捷键        │   │
│  │ (置顶/穿透)  │  │ (右键菜单)│  │ (隐藏/显示/唤醒)  │   │
│  └──────────────┘  └───────────┘  └───────────────────┘   │
├────────────────────────────────────────────────────────────┤
│                       Vue 前端层                           │
│  ┌──────────┐ ┌────────────┐ ┌──────────┐ ┌──────────┐    │
│  │ 角色渲染  │ │ 对话气泡   │ │ 交互菜单 │ │ 状态管理  │    │
│  │ (过渡切换)│ │ (打字机)   │ │ (点击反馈)│ │ (Pinia)  │    │
│  └──────────┘ └────────────┘ └──────────┘ └──────────┘    │
├────────────────────────────────────────────────────────────┤
│                    AI / Agent 能力层                        │
│  ┌──────────┐ ┌────────────┐ ┌──────────┐ ┌──────────┐    │
│  │ LLM 调用  │ │ Tool Call  │ │ 工具系统  │ │ 对话管理  │    │
│  │ (多Provider)│ │ (函数调用) │ │ (可扩展)  │ │ (上下文) │    │
│  └──────────┘ └────────────┘ └──────────┘ └──────────┘    │
└────────────────────────────────────────────────────────────┘
```

---

## 三、角色渲染方案（核心设计）

鉴于现有立绘是**独立的整幅 PNG** 而非动画序列帧，采用以下方案：

### 3.1 显示机制

- 每个立绘是一个独立的完整插图，CSS 控制显示/隐藏与过渡
- 使用 `opacity` + `transition` 实现立绘之间平滑切换（cross-fade）
- 角色状态决定当前显示的立绘（姿势 A / B → 具体变体）

### 3.2 状态体系

```
角色状态
├── idle_A     → 姿势 A 系列（随机选一张或循环切换）
├── idle_B     → 姿势 B 系列（带边框的变体）
├── talking    → 说话状态（可在同姿势内微切换，模拟动态）
└── thinking   → 思考/等待状态
```

- **idle 模式**：在一种姿势的多张图片间慢速轮换（如每 8-15 秒切换一张），配合淡入淡出，看起来像角色在自然活动
- **说话模式**：在当前姿势的 2-3 张变体间较快切换，配合对话气泡产生"在说话"的感觉
- **过渡方式**：所有切换均用 CSS transition（`opacity 0.6s ease`）+ 略微位移（`transform: translateY(5px)`），避免生硬跳变

### 3.3 选图策略

每一组立绘的变体都是同姿势的不同"状态"（眼神方向、表情微变），利用这一点实现伪动画效果：

| 立图片名 | 姿势 | 变体索引 | 用途 |
|----------|------|---------|------|
| `kisakiA_aa` ~ `kisakiA_al` | A | a(0) ~ l(11) | 主 idle、talking 切换 |
| `kisakiA_ba` ~ `kisakiA_bl` | A | a(0) ~ l(11) | 副 idle 变体 |
| `kisakiB_aa` ~ `kisakiB_br` | B | a(0) ~ r(17) | 第二姿势（带边框） |

---

## 四、分阶段实施

### 第一阶段：角色渲染与交互基础

**目标**：让角色在桌面显示并具备基本交互

#### 4.1 角色显示系统

| 文件 | 职责 |
|------|------|
| `src/character/` | 角色模块 |
| `src/character/index.ts` | 统一导出 |
| `src/character/config.ts` | 角色配置（图片路径映射、状态定义） |
| `src/character/controller.ts` | 状态管理与切换逻辑 |

- 加载 Kisaki A/B 姿势资源
- 实现 idle 状态下的图片慢速轮换（带 cross-fade 过渡）
- 区分 A 姿势（无边框）和 B 姿势（带边框）

#### 4.2 交互系统

- **拖拽**：整个窗口可拖拽（已有基础）
- **点击角色**：弹出输入框或触发反馈（轻微缩放动效）
- **右键菜单**：设置 / 切换角色 / 退出
- **鼠标穿透**：挂机模式时鼠标穿透窗口，不影响桌面操作

#### 4.3 对话气泡组件

```
┌───────────────────┐
│   (指向角色的角标) │
│                    │
│  你好！今天天气      │   ← 打字机逐字效果
│  不错呢~            │
│                    │
│                    │
└───────────────────┘
```

- 指向角色的 tail 气泡
- 打字机逐字输出
- 支持富文本（表情、链接）

| 文件 | 职责 |
|------|------|
| `src/components/Character.vue` | 角色立绘渲染（过渡切换） |
| `src/components/DialogueBubble.vue` | 对话气泡（打字机效果） |
| `src/components/ContextMenu.vue` | 右键菜单 |
| `src/components/InputBox.vue` | AI 对话输入框 |

---

### 第二阶段：AI 对话能力

**目标**：角色能与用户自然对话

#### 2.1 LLM API 集成

- 支持多 Provider：OpenAI / Anthropic / 兼容 API
- API 基础地址 + Key 配置
- 流式（SSE/Stream）响应，配合打字机效果

#### 2.2 角色人设系统

- System Prompt 定义角色性格、说话风格
- 可配置的角色设定（名称、性格、知识背景）
- 对话历史管理（滑动窗口，保留最近 N 轮）

#### 2.3 配置管理

- API 设置页面
- 角色选择界面
- 本地持久化（Tauri store / localStorage）

| 文件 | 职责 |
|------|------|
| `src/ai/` | AI 模块 |
| `src/ai/client.ts` | API 调用封装（多 Provider） |
| `src/ai/stream.ts` | 流式响应处理 |
| `src/ai/prompts.ts` | 角色人设模板 |
| `src/ai/context.ts` | 对话上下文管理 |
| `src/stores/` | Pinia 状态管理 |
| `src/stores/settings.ts` | 设置状态 |
| `src/stores/chat.ts` | 对话状态 |

---

### 第三阶段：Agent 能力

**目标**：桌宠能执行实际操作，不只是聊天

#### 3.1 Function Calling 机制

基于 LLM 的 Tool Use 标准接口实现：

```
用户输入 ─→ LLM 生成 tool_call ─→ 执行本地工具 ─→ 结果返回 LLM ─→ 自然语言回复
```

#### 3.2 工具清单（逐步实现）

| 工具 | 描述 | 实现方式 | 优先级 |
|------|------|---------|--------|
| `get_time` | 获取当前时间/日期 | JS Date | P0 |
| `get_weather` | 查询天气 | 调用天气 API | P0 |
| `calc` | 计算器 | JS 表达式求值 | P1 |
| `search_web` | 搜索信息 | 调用搜索 API | P1 |
| `take_note` | 记录笔记 | Tauri FS 写入本地文件 | P1 |
| `read_file` | 读取文件内容 | Tauri FS API | P1 |
| `open_file` | 打开文件/资源管理器 | Tauri shell API | P1 |
| `set_reminder` | 设置提醒 | 本地定时器 | P2 |
| `clipboard` | 读写剪贴板 | Tauri clipboard API | P2 |
| `screenshot` | 截图 | Tauri 插件或命令行 | P2 |

#### 3.3 工具架构

```
src/agent/
├── index.ts          # 统一导出
├── types.ts          # Agent 类型定义（Tool 接口、Call 结构）
├── registry.ts       # 工具注册中心
├── router.ts         # 工具调用路由（LLM response → 工具匹配）
└── tools/            # 工具实现
    ├── time.ts
    ├── weather.ts
    ├── calculator.ts
    ├── search.ts
    ├── notes.ts
    └── ...
```

#### 3.4 安全性考量

- 危险操作（文件读写、命令执行）须用户确认
- 工具列表完全可控，仅执行已注册的函数
- Tauri 权限配置精细化管理

---

### 第四阶段：体验完善

| 特性 | 说明 |
|------|------|
| 系统托盘 | 托盘图标，右键菜单（显示/隐藏/退出） |
| 开机自启 | Tauri autostart 插件 |
| 多角色切换 | 不同角色立绘包+不同人设 |
| 记忆系统 | 跨对话记住用户信息、偏好 |
| 皮肤/主题 | 气泡样式、菜单样式自定义 |
| Live2D 升级 | 未来可引入 Live2D 模型（`pixi-live2d-display` 已安装） |

---

## 五、技术栈

| 层 | 技术选型 | 理由 |
|----|----------|------|
| 桌面框架 | Tauri v2 | 已在用，Rust 后端，跨平台 |
| 前端框架 | Vue 3 + TypeScript | 已在用 |
| 状态管理 | Pinia | Vue 3 官方推荐 |
| 动画 | CSS transition + requestAnimationFrame | 立绘切换无需重型引擎 |
| AI API | OpenAI SDK / Anthropic SDK | Function Calling + Stream |
| HTTP | fetch (原生) | 轻量无额外依赖 |
| 本地存储 | Tauri Store Plugin | 持久化配置/对话记录 |
| 图标 | Tauri icons | 已在用 |

---

## 六、推荐的目录结构

```
FloatWindow/
├── src/
│   ├── main.ts                    # 入口
│   ├── App.vue                    # 根组件
│   ├── components/
│   │   ├── Character.vue          # 角色立绘渲染（含过渡切换）
│   │   ├── DialogueBubble.vue     # 对话气泡（打字机效果）
│   │   ├── InputBox.vue           # 对话输入框
│   │   └── ContextMenu.vue        # 右键菜单
│   ├── character/
│   │   ├── index.ts               # 导出
│   │   ├── config.ts              # 角色资源配置（图片路径、状态映射）
│   │   └── controller.ts          # 角色状态控制（idle轮换、说话切换）
│   ├── ai/
│   │   ├── index.ts               # 导出
│   │   ├── client.ts              # API 调用（多 Provider 适配）
│   │   ├── stream.ts              # 流式响应处理
│   │   ├── prompts.ts             # 角色人设提示词
│   │   └── context.ts             # 对话上下文管理
│   ├── agent/
│   │   ├── index.ts               # 导出
│   │   ├── types.ts               # 类型定义
│   │   ├── registry.ts            # 工具注册表
│   │   ├── router.ts              # 工具调用路由
│   │   └── tools/
│   │       ├── time.ts
│   │       ├── weather.ts
│   │       ├── calculator.ts
│   │       ├── search.ts
│   │       └── notes.ts
│   ├── stores/
│   │   ├── settings.ts            # 设置状态（Pinia）
│   │   └── chat.ts                # 对话状态（Pinia）
│   └── utils/
│       ├── storage.ts             # 持久化工具
│       └── config.ts              # 配置管理
├── public/
│   └── character/
│       └── kisaki/                # 现有立绘资源（不变）
├── src-tauri/                     # Tauri 后端（不变）
├── 项目规划.md                     # 本文件
└── ...
```

---

## 七、开发路线图

```
第1周 ─── 角色立绘系统（config + controller + Character.vue）
          实现 idle 慢速轮换、cross-fade 过渡、基本显示

第2周 ─── 交互功能（拖拽增强、右键菜单、输入框、对话气泡）
          角色点击弹出输入，气泡显示文字

第3周 ─── AI 对话集成（API 调用、流式响应、角色人设）
          实现与 LLM 对话，打字机输出

第4周 ─── Agent 能力（Tool Calling 架构、基础工具）
          时间、天气、计算器、搜索

第5周 ─── 体验打磨（系统托盘、配置界面、状态记忆）
          持久化、多角色切换、设置界面
```

---

## 八、当前立绘资源索引

> 确认现有资源，便于编码引用

### KisakiA（姿势 A，无边框）

| 前缀 | 文件数 | 命名规则 |
|------|--------|---------|
| `kisakiA_a` | 12 张 | `kisakiA_aa` ~ `kisakiA_al` |
| `kisakiA_b` | 12 张 | `kisakiA_ba` ~ `kisakiA_bl` |

### KisakiB（姿势 B，带边框）

| 前缀 | 文件数 | 命名规则 |
|------|--------|---------|
| `kisakiB_a` | 18 张 | `kisakiB_aa` ~ `kisakiB_ar` |
| `kisakiB_b` | 18 张 | `kisakiB_ba` ~ `kisakiB_br` |

---

## 九、关键设计决策

1. **立绘切换用 CSS transition 而非 PixiJS**：因为图片是独立整幅而非序列帧，CSS cross-fade 足以实现平滑过渡，无需引入重型渲染引擎
2. **状态机驱动**：角色所有行为（idle/talk/think）由状态机统一控制，UI 层只响应状态变化
3. **API 无关设计**：AI Provider 抽象为统一接口，支持切换 OpenAI / Anthropic / 兼容 API
4. **工具即函数**：Agent 工具注册为纯函数，每个工具独立文件，方便扩展和测试
5. **安全优先**：所有涉及文件系统/命令执行的工具默认需要用户确认
